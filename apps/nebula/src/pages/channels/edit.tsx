import { Page } from "@nebula/ui/components";
import ChannelsNav from "@nebula/ui/features/channels/navs/ChannelsNav";

export default function UtmPage() {
  return <Page navigation={<ChannelsNav />}></Page>;
}
